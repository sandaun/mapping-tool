# ============================================================
# Excel Mapping Tool — Environment Variables
# ============================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
# ============================================================


# ------------------------------------------------------------
# AI Provider Configuration
# ------------------------------------------------------------
# Hybrid strategy:
#   - PDFs       → always OpenAI (vision capability required)
#   - Excel/CSV  → Cerebras (free & fast)
#
# Choose active provider: openai | cerebras | groq
AI_PROVIDER=cerebras

# Model override (optional — each provider has sensible defaults)
#   OpenAI:   gpt-4o
#   Cerebras: llama3.1-8b
#   Groq:     llama-3.3-70b-versatile
# AI_MODEL=


# ------------------------------------------------------------
# API Keys — add only the ones you need
# ------------------------------------------------------------

# OpenAI (required for PDF parsing via vision)
# Get your key: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-key-here

# Cerebras (free tier available)
# Get your key: https://cloud.cerebras.ai
CEREBRAS_API_KEY=csk-your-cerebras-key-here

# Groq (free tier available, optional)
# Get your key: https://console.groq.com/keys
# GROQ_API_KEY=gsk-your-groq-key-here


# ------------------------------------------------------------
# Supabase (Auth + Signal Library)
# ------------------------------------------------------------
# Find these in: Supabase Dashboard → Settings → API
# Auth is handled by Supabase Auth — no extra config needed.
# Create users in: Supabase Dashboard → Authentication → Users

# Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Publishable (anon) key — safe for browser use, used for auth session
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=sb_publishable_your-key-here

# Secret key — server-only, bypasses RLS. NEVER expose to browser.
SUPABASE_SECRET_KEY=sb_secret_your-key-here
